diff --git a/.github/workflows/autofix-pr.yml b/.github/workflows/autofix-pr.yml
index f912cd8cf4912afd22e6eec3847d6a4d06bfc7d2..850e67ba76648c73709e68bdbc95c0c1b01d189e 100644
--- a/.github/workflows/autofix-pr.yml
+++ b/.github/workflows/autofix-pr.yml
@@ -1,87 +1,39 @@
-name: autofix-pr
+name: Autofix PR
 
 on:
-  workflow_dispatch: {}  # 수동 실행만
+  pull_request:
+  workflow_dispatch:
 
 permissions:
   contents: write
   pull-requests: write
 
-concurrency:
-  group: "autofix"
-  cancel-in-progress: true
-
 jobs:
   autofix:
     runs-on: ubuntu-latest
+
     steps:
-      - name: Checkout main
+      - name: Checkout
         uses: actions/checkout@v4
         with:
-          ref: main
           fetch-depth: 0
-          persist-credentials: true
-          token: ${{ secrets.GITHUB_TOKEN }}
-
-      - name: Configure Git (safe)
-        run: |
-          git config --global user.name "github-actions[bot]"
-          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
-          git config --global --add safe.directory "$GITHUB_WORKSPACE"
 
       - name: Setup Node
         uses: actions/setup-node@v4
         with:
-          node-version: "20"
+          node-version: 20
+          cache: npm
 
-      # 의존성 설치 (실패해도 계속 진행)
-      - name: Install deps (never fail)
-        shell: bash
-        run: |
-          if [ -f pnpm-lock.yaml ]; then corepack enable && pnpm i || true
-          elif [ -f yarn.lock ]; then corepack enable && yarn i || true
-          elif [ -f package-lock.json ]; then npm ci || npm i || true
-          elif [ -f package.json ]; then npm i || true
-          else echo "No package.json"; fi
+      - name: Install dependencies
+        run: npm ci
 
-      # 자동 포맷/린트 (실패해도 계속 진행)
-      - name: Auto-fix (Prettier/ESLint — safe)
-        shell: bash
-        run: |
-          npx --yes prettier . --write || true
-          npx --yes eslint . --ext .js,.jsx,.ts,.tsx --fix || true
+      - name: Run formatter
+        run: npx --yes prettier . --write
 
-      # 변경 있을 때만 PR 생성
-      - name: Detect changes (never fail)
-        id: detect
-        shell: bash
-        run: |
-          set +e
-          git add -A
-          if git diff --staged --quiet; then
-            echo "changed=false" >> $GITHUB_OUTPUT
-            echo "No changes"
-          else
-            echo "changed=true" >> $GITHUB_OUTPUT
-            echo "Has changes"
-          fi
-          exit 0
+      - name: Run tests
+        run: npm test --if-present
 
-      - name: Create Pull Request (single rolling PR)
-        if: steps.detect.outputs.changed == 'true'
-        uses: peter-evans/create-pull-request@v6
+      - name: Commit changes
+        uses: stefanzweifel/git-auto-commit-action@v5
         with:
-          token: ${{ secrets.GITHUB_TOKEN }}
-          base: main
-          branch: bot/autofix          # 고정 → PR 1개만 유지
-          commit-message: "chore(autofix): apply automatic code fixes"
-          title: "chore(autofix): automatic code fixes"
-          body: "자동 포맷/린트 수정 PR입니다."
-          delete-branch: true
-          committer: "github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>"
-          author: "github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>"
-
-      - name: Nothing to commit
-        if: steps.detect.outputs.changed != 'true'
-        run: echo "No changes to commit."
-
+          commit_message: 'chore: apply automatic fixes'
